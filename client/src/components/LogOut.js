import React, { Component } from 'react';

export default class LogOut extends Component
{
    render() {
        return (
            <div>
                Log Out (Doesn't actually happen)
            </div>
        );
    }
}